name: 'github-action-aws-cdk-amazon-linux'
description: 'GitHub Action for running CDK in amazonlinux latest'
working_dir:
    description: 'AWS CDK working directory.'
    default: '.'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'upload-cloud'
  color: 'yellow'
inputs:
  cdk_command:
    description: 'CDK Command'
    default: '--version'
  working_dir:
    description: 'AWS CDK working directory.'
    default: '.'
